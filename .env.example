# Optional server-only crawler collection; never use NEXT_PUBLIC_ for this key.
TRACWELL_SERVER_KEY=

# Set to your Next.js deployment origin for workspace install URLs.
# NEXT_PUBLIC_SITE_URL=http://localhost:3000
