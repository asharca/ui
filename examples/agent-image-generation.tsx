'use client';
import { useState } from 'react';
import { ImageGeneration, type ImageGenerationStatus } from '@/components/asharca/agent-image-generation';
export default function Demo() { const [status, setStatus] = useState<ImageGenerationStatus>('generating'); return <div className="w-full max-w-md space-y-3"><ImageGeneration status={status} prompt="一张宁静的抽象渐变画面" label="本地图像生成状态演示" size="fluid" aspectRatio="4 / 3" onRetry={() => setStatus('generating')}><div className="grid h-full w-full place-items-center bg-gradient-to-br from-slate-100 via-indigo-100 to-emerald-100 text-slate-700"><span className="text-lg font-medium">本地视觉占位 · 不调用模型</span></div></ImageGeneration><div className="flex flex-wrap gap-2">{(['queued', 'generating', 'refining', 'complete', 'error'] as const).map((value) => <button key={value} aria-pressed={value === status} className="rounded-lg border border-border px-2 py-1.5 text-xs" onClick={() => setStatus(value)}>{value}</button>)}</div></div>; }
