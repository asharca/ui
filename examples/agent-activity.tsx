'use client';
import { useState } from 'react';
import { AgentActivity, type AgentActivityItem } from '@/components/asharca/agent-activity';
const items: AgentActivityItem[] = [{ id: 'read', type: 'step', label: '读取需求', status: 'complete' }, { id: 'text', type: 'text', content: '将源码适配、交互验证和文档同步分别处理。' }, { id: 'search', type: 'search', query: '组件安装与交互约定', results: [{ id: 'docs', title: 'Registry 文档', domain: 'ui.shadcn.com', url: 'https://ui.shadcn.com/docs/registry' }] }, { id: 'tool', type: 'tool', action: 'edit', target: 'components/task.tsx', additions: 12, deletions: 3 }, { id: 'trace', type: 'trace', kind: 'run', label: '验证组件', detail: '类型检查、单元测试与浏览器交互' }];
export default function Demo() { const [complete, setComplete] = useState(false); const [kind, setKind] = useState('mixed'); return <div className="w-full max-w-xl space-y-3"><div className="flex flex-wrap gap-2">{['mixed', 'step', 'text', 'search', 'tool', 'trace'].map((value) => <button key={value} aria-pressed={kind === value} className="rounded-lg border border-border px-2 py-1.5 text-xs" onClick={() => setKind(value)}>{value}</button>)}</div><AgentActivity items={kind === 'mixed' ? items : items.filter((item) => item.type === kind)} status={complete ? 'complete' : 'working'} duration={12.5} activeLabel="处理任务" summary="本地执行记录已完成" /><button className="text-xs underline" onClick={() => setComplete(!complete)}>{complete ? '重播活动' : '完成活动'}</button></div>; }
