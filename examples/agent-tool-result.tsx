'use client';
import { useState } from 'react';
import { ToolResult, ToolResultOutput, type ToolResultStatus } from '@/components/asharca/agent-tool-result';
export default function Demo() { const [status, setStatus] = useState<ToolResultStatus>('success'); return <div className="w-full max-w-xl space-y-3"><div className="flex gap-2">{(['running', 'success', 'error'] as const).map((value) => <button className="rounded-lg border border-border px-3 py-2 text-xs" key={value} aria-pressed={value === status} onClick={() => setStatus(value)}>{value}</button>)}</div><ToolResult tool="test_runner" title="运行组件测试" kind="terminal" status={status} defaultOpen collapseOnComplete={false} copyText="pnpm test" onRetry={() => setStatus('running')}><ToolResultOutput language="bash">{status === 'success' ? 'PASS: component interactions validated' : status === 'error' ? 'FAIL: connection unavailable; retry supported' : 'Running component tests…'}</ToolResultOutput></ToolResult></div>; }
