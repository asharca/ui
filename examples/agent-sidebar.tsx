'use client';
import { useState } from 'react';
import { AISidebar, AnimatedSidebarProvider, AnimatedSidebar, AnimatedSidebarContent, AnimatedSidebarHeader, AnimatedSidebarTrigger, type SidebarResource } from '@/components/asharca/agent-sidebar';
const initial: SidebarResource[] = [{ id: 'project', label: '组件迁移', kind: 'project', children: [{ id: 'chat', label: '对话记录', kind: 'file' }, { id: 'spec', label: '实现说明.md', kind: 'file' }] }, { id: 'archive', label: '归档', kind: 'folder' }];
export default function Demo() { const [items, setItems] = useState(initial); const [active, setActive] = useState('chat'); return <AnimatedSidebarProvider className="h-[380px] min-h-0 w-full max-w-2xl overflow-hidden rounded-xl border border-border"><AnimatedSidebar className="h-full" panelClassName="h-full" collapsible="offcanvas" ariaLabel="Agent 资源"><AnimatedSidebarHeader><p className="px-2 text-sm font-semibold">Agent 工作区</p></AnimatedSidebarHeader><AnimatedSidebarContent><AISidebar items={items} onItemsChange={setItems} activeId={active} onActiveChange={setActive} defaultExpandedIds={['project']} onRename={async () => undefined} /></AnimatedSidebarContent></AnimatedSidebar><div className="min-w-0 flex-1 p-3"><AnimatedSidebarTrigger aria-label="切换 Agent 导航">☰</AnimatedSidebarTrigger><p className="mt-4 text-sm">当前资源：{active}</p><p className="mt-2 text-xs text-muted-foreground">支持重命名、拖动与键盘调整资源位置。</p></div></AnimatedSidebarProvider>; }
